# Model Weights

Store only small metadata and download instructions here.

Do not commit large YOLO weights, datasets, ROS bags, or generated model artifacts directly to normal Git history. Use Git LFS, DVC, GitHub Releases, or external storage once the model workflow is decided.
