2026-08-13 20:45 BST
Decision: Evaluate Nav2 costmap infrastructure before further optimizing the
project's Python grid generator.
Philosophy: Reuse mature infrastructure where it fits, but measure it on the
actual Foxy/Jetson/AIRY combination instead of assuming it solves the freeze.

2026-08-13 20:52 BST
Decision: Keep modern Nav2 Collision Monitor out of the Foxy implementation.
Philosophy: Collision Monitor matches direct stop/slow sensing conceptually,
but backporting a post-Foxy control component would expand scope and duplicate
the already validated operator-intent and Pi enforcement pipeline.

2026-08-13 20:59 BST
Decision: First launch the unmodified Foxy ObstacleLayer on raw AIRY data and
publish it on isolated /nav2_front_costmap.
Philosophy: Establish a clean stability and map-behaviour baseline before
adding artifact filters, timestamp adapters, or safety-supervisor coupling.

2026-08-13 21:05 BST
Decision: Treat the first Nav2 result as publication continuity, not map
latency, and remove any multi-scan arming requirement.
Philosophy: Foxy's visual grid does not retain the sensor acquisition stamp.
The experiment should state only what it measures and let the first valid map
appear without an invented five-scan policy.

2026-08-14 17:40 BST
Decision: Reintroduce the calibrated AIRY artifact filter as a standalone
PointCloud2 stage upstream of an otherwise-unmodified Nav2 ObstacleLayer.
Philosophy: Keep the stable mapping implementation and the project-specific
sensor correction separate so each component has one clear responsibility.

2026-08-14 17:40 BST
Decision: Preserve every retained AIRY point record, source-frame coordinate,
and acquisition stamp instead of rebuilding an XYZ-only cloud.
Philosophy: Filtering should remove known bad observations without discarding
diagnostic fields or obscuring where and when the sensor measured good ones.

2026-08-14 17:40 BST
Decision: Stop filtered publication on invalid input, timestamps, transforms,
or calibration rather than silently passing raw points through.
Philosophy: A visible missing-input fault is safer and easier to diagnose than
quietly reintroducing artifacts into a map labelled as filtered.

2026-08-14 17:40 BST
Decision: Make filtered input the Nav2 default while retaining an explicit raw
A/B launch switch and keeping physical shared control disconnected.
Philosophy: Use the only practically useful map for normal observation, retain
a cheap diagnostic comparison, and validate stability before actuation.

2026-08-14 18:03 BST
Decision: Evaluate calibrated cells only for the subset of points eligible for
front marking, while retaining other finite non-self points for Nav2.
Philosophy: Spend CPU where the filter can change a decision without taking
height, range, or clearing policy away from Nav2.

2026-08-14 18:03 BST
Decision: Populate PointCloud2 byte data with Python's native unsigned-byte
array rather than assigning bytes through Foxy's generated message setter.
Philosophy: Preserve the full AIRY schema while avoiding a hidden second
per-byte conversion that cost roughly 80 ms per recorded cloud on the Jetson.

2026-08-14 18:03 BST
Decision: Build debug clouds only when subscribed, publish static markers once,
and rate-limit changing threshold markers and success diagnostics.
Philosophy: Diagnostic detail should remain available on demand without taxing
the headless production path every LiDAR callback.

2026-08-14 18:03 BST
Result: A warmed recorded-rate replay measured raw-to-filter delay p95 80.45 ms
and p99 93.02 ms; Nav2 published at 6.42 Hz with a 231.56 ms maximum map gap.
Philosophy: Record the bounded replay result as evidence, while reserving the
six-minute live run as the production endurance gate.

2026-08-14 19:13 BST
Decision: Register Foxy's InflationLayer behind a default-off launch toggle,
with a 0.55 m radius and 3.0 scaling factor exposed for A/B tuning.
Philosophy: Make Nav2's weighted representation easy to inspect without
changing the performance-validated obstacle-only default.

2026-08-14 19:13 BST
Decision: Keep optional inflation on the isolated /nav2_front_costmap and do
not connect it to /front_costmap or the safety supervisor.
Philosophy: Graded costs need an explicit CLEAR/SLOW/STOP interpretation and
freshness contract before they are allowed to affect physical shared control.

2026-08-14 19:33 BST
Decision: Apply inflation launch arguments by rewriting the exact Foxy Nav2
parameter section while preserving bool and float types.
Philosophy: A launch banner is not proof of runtime configuration; verify the
live lifecycle node and work with Foxy's exact-node parameter precedence.

2026-08-18 18:42 BST
Decision: Replace the supervisor's binary occupied-point input with weighted
cost sampling from /nav2_front_costmap, while retaining explicit legacy replay
wiring on /front_costmap.
Philosophy: Integrate one proven boundary at a time: first make Nav2's graded
representation observable in shadow decisions without changing joystick,
UDP, or message contracts.

2026-08-18 18:42 BST
Decision: Sample Nav2 costs at prospective robot-centre poses from straight
through the requested correction, using initial SLOW 1..98 and STOP 99..100
bands inside the existing 1.20 m and 0.70 m horizons.
Philosophy: Nav2 inflation is already configuration-space evidence, so
sweeping the physical footprint over it again would double-count chair size
and make tight passages unnecessarily restrictive.

2026-08-18 18:42 BST
Decision: Keep OccupancyGrid publication time as the provisional freshness
input for this cost-policy shadow stage and keep physical enforcement out.
Philosophy: Cost semantics can be measured independently, but a visually
credible stale map must not later be mistaken for fresh LiDAR evidence.

2026-08-18 19:51 BST
Decision: Replace live reliance on Foxy's zero-stamped Nav2 OccupancyGrid with
two watchdogs: monotonic costmap receipt age and a lightweight heartbeat that
retains the successfully filtered AIRY acquisition stamp.
Philosophy: Preserve fail-closed source evidence without copying PointCloud2
into the supervisor or pretending Nav2 retains one-scan-to-one-map provenance.

2026-08-18 19:51 BST
Decision: Keep legacy recorded-map replay on its explicit restamped-header
mode while making dual-watchdog Nav2 freshness the production default.
Philosophy: Avoid invalidating proven decision replay while keeping replay
timestamps clearly separate from physical latency evidence.
