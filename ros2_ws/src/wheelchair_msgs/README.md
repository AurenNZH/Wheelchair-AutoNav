# Wheelchair Messages

Message-only package defining operator intent, a short-lived internal Nav2
steering suggestion, and the fail-safe safety envelope. Operator intent carries
an explicit steering-assist authority; zero preserves direct shared control.
It contains no actuator code.
